This is the local **un-stage** path — review, drop, or discard atoms already staged (and the
escape valve when the hard capture-budget cap is blocking new captures). It does **no**
extraction, scoring, or staging: